# 折扣管理原型检测记录

## 检测对象

- 线上地址：https://favours-ff.github.io/prototype-hosting/prototypes/erp/prototype-20260602-183445/
- 本地调整副本：prototype/erp-20260602-183445-adjusted/

## 检测结果

- 页面入口可访问，本地访问状态码 200。
- 静态资源存在：index.html、JS、CSS、manifest.json、prd-yunxiao-copy.html。
- React 根节点可正常渲染。
- 首屏包含：数据卡片、汇总趋势图、折扣活动列表。
- 新建活动入口可打开，产品折扣、多买多省、买X送Y类型说明与示例可展示。
- 活动详情、审批详情入口可打开。
- 未发现阻断型 JS runtime error。
- 图表组件存在一次初始化尺寸 warning，最终图表容器尺寸正常，已增加 CSS 兜底。

## 已调整内容

- 将 HTML `lang` 从 `en` 改为 `zh-CN`。
- 将浏览器标题从 `My Google AI Studio App` 改为 `折扣管理原型`。
- 增加 Recharts 容器最小尺寸兜底，降低首次渲染空图风险。

## 注意事项

- 当前副本是线上构建后的静态产物，不是 React 源码工程。
- 大范围业务交互调整建议回到原始源码目录修改后重新构建，避免直接改压缩 JS 导致回归风险。

## 2026-08-06 浏览器批注调整

- 将顶部指标卡「失败绑定待重试」改为「活动带来订单数量」，示例值为 `2,450`，说明为「统计活动归因订单」。
- 将顶部指标卡「本期支持平台」改为「活动带来的销售额」，示例值为 `฿128,450`，说明为「统计活动归因销售额」。
- 将活动列表店铺字段改为单店铺展示，去除 `等11个`、`等14个` 等多店铺表达。
- 将详情页「生效店铺」同步改为单店铺展示，不再展示多店铺数量、更多店铺和备用店铺列表。
- 新建活动页店铺选择标题去除「最多 15 个」表述。
- 已通过浏览器复测：首屏、列表、详情页均无阻断型 JS 错误。

## 2026-08-06 侧边栏调整

- 按浏览器批注移除左侧侧边栏区域。
- 主内容区域自动占满可用宽度，左边界从页面左侧开始。
- 已通过浏览器复测：侧边栏 `display:none`，首屏指标卡保留，页面无阻断型 JS 错误。

## 2026-08-06 表格展示调整

- 店铺列从可点击下拉样式改为普通文本展示。
- 活动名称下方只展示状态标签，去除 `#1/#2/#3` 等序号。
- 已通过浏览器复测：列表店铺不可交互、序号不再展示、页面无阻断型 JS 错误。

## 2026-08-06 详情页模块调整

- 移除详情页基础设置下方的三栏模块：状态映射、接口与异常追踪、审批详情。
- 保留活动商品概览、确认并保存设置、促销商品明细等后续内容。
- 已通过浏览器复测：目标模块不可见，商品概览仍展示，页面无阻断型 JS 错误。

## 2026-08-06 详情页视觉优化

- 收紧详情页主内容 padding、卡片间距、表格行高，减少大面积留白。
- 优化详情页背景、卡片边框、阴影、输入框聚焦态和表格 hover 状态，使整体 UI 更精致。
- 商品明细区输入框宽度和后缀位置已调整，避免数字与符号重叠。
- 将错误后缀 `%离` 修正为 `%`。
- 已通过浏览器复测：详情页商品明细存在、三栏废弃模块隐藏、无阻断型 JS 错误。

## 2026-08-06 新增/详情抽屉化调整

- 参考线上 TikTok 广告管理新增/编辑页：右侧 Ant Design 抽屉，约 `960px` 宽，顶部标题栏，正文纵向表单区块，底部操作区。
- 将新增折扣活动页从整页形态调整为右侧半弹窗抽屉，保留活动类型、基础信息、规则配置、下一步等原有交互。
- 将折扣活动详情页调整为右侧半弹窗抽屉，保留详情信息、商品明细、编辑/返回等原有交互。
- 新增页面状态识别脚本，仅在新增页或详情页时启用抽屉样式，列表页不受影响。
- 更新静态资源缓存版本为 `comments-20260806-9`。
- 已通过浏览器复测：列表页无抽屉 class；新增页/详情页均为 `960px` 右侧抽屉，右边界贴齐，底部操作区保留，无阻断型 JS 错误。

## 2026-08-06 新增/编辑抽屉内部布局调整

- 新增页不再只固定抽屉尺寸，已调整抽屉内部组件排列。
- 将「活动类型选择」从左右挤压布局改为单列横向说明卡片，每张卡片占满抽屉内容宽度。
- 将「投放设置」独立为两列表单区，活动名称、时间、店铺选择等长内容横跨整行，减少字段挤压。
- 将「优惠规则配置」改为独立纵向区块，规则输入项扩展为两列大输入框，避免 180px 窄输入框。
- 详情/编辑页商品明细表格增加横向工作宽度，避免价格、折扣率、状态等列挤在一起。
- 更新静态资源缓存版本为 `comments-20260806-13`。
- 已通过浏览器复测：新增页类型卡片宽度约 `878px`，规则输入框约 `401px`；详情页抽屉右边界贴齐，商品表格保持可横向承载。

## 2026-08-06 详情页重新设计

- 原详情页布局错乱，已不再沿用旧 DOM 排版，改为在详情抽屉中渲染新的详情结构。
- 新结构拆分为：抽屉标题栏、活动概览卡、基本设置卡、促销商品明细卡、底部操作栏。
- 活动概览卡展示活动名称、状态、活动 ID、平台站点、活动类型、生效店铺、活动销售额和订单数。
- 基本设置卡采用两列表单读取态，备注横跨整行，避免字段竖排和错位。
- 商品明细卡采用工具栏 + 横向表格，按主商品 / 变体分层展示 SKU、库存、限购、折后价、折扣率和状态。
- 隐藏旧详情页错乱内容，保留返回列表、开始编辑活动的原有交互触发。
- 更新静态资源缓存版本为 `comments-20260806-18`。
- 已通过浏览器复测：新详情结构存在，旧内容可见数为 0，抽屉右边界和底部操作栏均贴齐视口边界。

## 2026-08-06 详情页批注精简

- 移除详情抽屉顶部标题栏，不再展示重复的活动名称、状态、活动 ID 和关闭按钮区域。
- 移除活动概览卡中的「有效时间」字段，避免与下方「基本设置」中的开始时间、结束时间重复。
- 修复详情页识别逻辑：去掉顶部活动 ID 后，仍能稳定保持详情抽屉状态。
- 修复详情结构注入方式，不再依赖 `document.createElement`，改用 `insertAdjacentHTML`。
- 更新静态资源缓存版本为 `comments-20260806-21`。
- 已通过浏览器复测：顶部栏不存在，概览区不包含有效时间，基本设置仍包含开始时间与结束时间，旧内容可见数为 0。
